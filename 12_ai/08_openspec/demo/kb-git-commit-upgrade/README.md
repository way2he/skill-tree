# Demo B: kb_git_commit.py 升级

> 🚧 占位目录，Day 7 启动

## 项目背景
给根目录的 `kb_git_commit.py` 加 3 个新功能，**不修改原工具**，只用 OpenSpec 规划 spec。

## OpenSpec Changes 规划
1. `add-commit-classify-preview` — 提交前预览分类
2. `add-auto-push-strategy` — 智能推送策略
3. `add-rollback-on-fail` — 失败回滚

## 启动命令（Day 7 执行）
```bash
cd demo/kb-git-commit-upgrade
openspec init
openspec new add-commit-classify-preview
```
