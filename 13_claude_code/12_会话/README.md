# 上下文溢出
当会话提示 "Context left until auto-compact: 3%" 说明历史记录快满了。
此时会自动触发压缩(约150s）也可手动用 /compact 命令续命，避免对话中断。

# 恢复与查看历史对话
如果你中途退出，或者是由于某些原因将Cluade code强制关闭了，可以使用下面的命令恢复历史对话：        claude -c 直接进入上次对话；claude -r 打开历史对话记录（使用较多）；

# 批量执行任务
在项目根目录下创建TASK.md文件，每一行写一个任务使用提示词：读取TASK.md中的内容，每一行是一个任务，依次执行这些任务